
---
- name: Test Notification
  hosts: all

  tasks:
    - name: Update and upgrade apt packages  
      ansible.builtin.uri:
        url: https://notify.federicohome.com/
        method: POST
        body_format: json
        body:
          token: A_c68XWoVfwsa63
          message: "Disk space on {{ inventory_hostname }} is above 90%!"
